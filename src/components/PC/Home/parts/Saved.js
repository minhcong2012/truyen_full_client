import React from 'react';
import PropTypes from 'prop-types';

const Saved = () => (
  <div className="Saved">
    Saved Component
  </div>
);

Saved.propTypes = {};

Saved.defaultProps = {};

export default Saved;
