import React from 'react';
import PropTypes from 'prop-types';

const Update = () => (
  <div className="Update">
    Update Component
  </div>
);

Update.propTypes = {};

Update.defaultProps = {};

export default Update;
